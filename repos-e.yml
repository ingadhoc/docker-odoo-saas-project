# Ingadhoc
ingadhoc-odoo-saas:
    defaults:
        depth: 1
    remotes:
        origin: https://$GITHUB_USER:$GITHUB_TOKEN@gitlab.com/ingadhoc/odoo-saas.git
    merges:
        - origin $ODOO_VERSION
    target: origin $ODOO_VERSION

ingadhoc-adhoc-saas:
    defaults:
        depth: 1
    remotes:
        origin: https://$GITHUB_USER:$GITHUB_TOKEN@gitlab.com/ingadhoc/adhoc-saas.git
    merges:
        - origin $ODOO_VERSION
    target: origin $ODOO_VERSION
